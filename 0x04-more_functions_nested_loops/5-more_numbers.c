#include "main.h"

/**
 * more_numbers - printing more numbers
 */

void more_numbers(void)
{
	int i, j;

	for (i = 1; i < 10; 1++)
	{
		for (j = 0; j <= 14; j++)
		{
			if (j >= 100
					_putchar(1);
			_putchar(j % 10 + '0');
		}
			_put char('\n')
	}
}
